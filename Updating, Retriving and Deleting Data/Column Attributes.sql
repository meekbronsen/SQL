# Datatype Int -- only takes intergers no floats 
# Datatype TINYINT  -- takes one byte of memory and can store  0 - 255
# Datatype VarChar(50)  -- variablecharacterstring. takes max of 50 characters and doest not waste space like datatype char does
                        -- most of the time we use varchar unless the character length is fixed
                        -- can be used for storing names, messages, phone numbers (variable length strings)
# PK Primary Key column
# NN Not Null this column cannot be null
# AI Auto Increment this column keeps track of the primary key column as data is being fed to the pk column